"""Публичные ошибки ядра, общие для нескольких модулей.

Живут отдельно от `app.py`, чтобы коллабораторы ядра (PermissionRelay и др.)
могли поднимать `UserError` без циклического импорта через god-object. `app.py`
реэкспортит `UserError` для обратной совместимости — адаптеры и тесты продолжают
импортировать его как `from ...core.app import UserError`.
"""

from __future__ import annotations


class UserError(Exception):
    """Ошибка с готовым текстом для пользователя (адаптер показывает как есть)."""


class ReplyRejected(Exception):
    """Ответ Claude не принят: текст исключения адресован МОДЕЛИ, не человеку.

    Отдельный тип нужен, чтобы reply-сервер отличил «модель промахнулась
    адресом» (это чинится повторным вызовом тула — отдаём 400 с объяснением)
    от аварии обработчика (500). Раньше кривой context_id молча ронялся: модель
    получала «Reply sent», оператор — ничего. Молчание тут хуже ошибки: сессия
    считала, что ответила, и строила дальнейшие рассуждения на несуществующей
    доставке (живой случай — выдуманный «личный канал»).
    """
